<!---Badge.stories.mdx --->

import {
  Meta,
  Story,
  Preview,
  Source,
  Props,
  ArgsTable,
} from '@storybook/addon-docs';

import TextArea from '../../../../src/elements/TextArea';

<Meta title="Components/Elements/TextArea" component={TextArea} />

export const Template = (args) => <TextArea {...args} />;

# Text Area

<Source language="jsx" code="import { TextArea } from 'bycoders-ui-react'" />

The `TextArea` component is a default `textarea` input and could be used in a Form or
outside it. It extends any default `HTMLTextAreaElement` props.

<br></br>

## Properties

<ArgsTable of={TextArea} />

## Default

In most of cases you should use it in this way:

<Preview>
  <Story
    name="Default"
    args={{
      label: 'Some Label',
      name: 'input-name',
      caption: 'Some Caption',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="Default" />

The `TextArea` component also supports a built-in save button with a handler.

<Preview>
  <Story
    name="Save Button"
    args={{
      label: 'Some Label',
      name: 'input-name',
      caption: 'Some Caption',
      enableButton: true,
      labelButton: 'Save',
      onSave: () => alert('button triggered'),
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="Save Button" />
