<!---Badge.stories.mdx --->

import {
  Meta,
  Story,
  Preview,
  Source,
  Props,
  ArgsTable,
} from '@storybook/addon-docs';

import Input from '../../../../src/elements/Input';

<Meta title="Components/Elements/Input" component={Input} />

export const Template = (args) => <Input {...args} />;

# Input

<Source language="jsx" code="import { Input } from 'bycoders-ui-react'" />

A `Input` is a default input type and could be used in a Form or outside it. It extends
any default `HTMLInput` props.

<br></br>

## Properties

<ArgsTable of={Input} />

## Default

In most of cases you should use it in this way:

<Preview>
  <Story
    name="Default"
    args={{
      label: 'Some Label',
      name: 'input-name',
      caption: 'Some Caption',
      disabled: false,
      type: 'text'
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="Default" />
