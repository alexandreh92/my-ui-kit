<!---Badge.stories.mdx --->

import {
  Meta,
  Story,
  Preview,
  Source,
  Props,
  ArgsTable,
} from '@storybook/addon-docs';

import RadioButton from '../../../../src/elements/RadioButton';

<Meta title="Components/Elements/RadioButton" component={RadioButton} />

export const Template = (args) => <RadioButton {...args} />;

# RadioButton

<Source language="jsx" code="import { RadioButton } from 'bycoders-ui-react'" />

A `RadioButton` is a radio input type and could be used in a Form or outside it. It extends
any default `HTMLInput` props.

<br></br>

## Properties

<ArgsTable of={RadioButton} />

## Default

In most of cases you should use it in this way:

<Preview>
  <Story
    name="Default"
    args={{
      label: 'Some Label',
      name: 'radio-name',
      caption: 'Some Caption',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="Default" />
