<!---Badge.stories.mdx --->

import {
  Meta,
  Story,
  Preview,
  Source,
  Props,
  ArgsTable,
} from '@storybook/addon-docs';

import Scala from '../../../../src/elements/Scala';

<Meta title="Components/Elements/Scala" component={Scala} />

export const Template = (args) => <Scala {...args} />;

# Scala

<Source language="jsx" code="import { Scala } from 'bycoders-ui-react'" />

The `Scala` component renders `RadioButton` components as a scale between
`min` and `max` props.

<br></br>

## Properties

<ArgsTable of={Scala} />

## Default

You should use it in this way:

<Preview>
  <Story
    name="Default"
    args={{
      label: 'Some Label',
      name: 'scala-name',
      caption: 'Some Caption',
      min: 1,
      max: 5,
      minLabel: 'Min Label',
      maxLabel: 'Max Label',
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="Default" />
