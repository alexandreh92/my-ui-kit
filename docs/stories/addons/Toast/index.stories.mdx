<!---Badge.stories.mdx --->

import {
  Meta,
  Story,
  Preview,
  Source,
  Props,
  ArgsTable,
} from '@storybook/addon-docs';

import Toast from '../../../../src/addons/Toast';

<Meta title="Components/Addons/Toast" component={Toast} />

export const Template = (args) => <Toast {...args} />;

# Toast

<br></br>

The `Toast` component is automatically rendered inside the `ToastContext` provider.

<br></br>

## Properties

<ArgsTable of={Toast} />

## Default

You can see it's variations below:

<Preview>
  <Story
    name="Default"
    args={{
      title: 'Some Title',
      message: 'Message',
      mode: 'success',
      persistent: true,
    }}
  >
    {Template.bind({})}
  </Story>
</Preview>

<ArgsTable story="Default" />

<br></br>
